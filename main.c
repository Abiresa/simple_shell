#include <stdio.h>
#include <stdlib.h>
#include "shell.h"

/*
 * main - Sets up a loop that continuously prompts for user input with the ">> " prompt.
 */

int main(void)
{
	shell_loop();

	return (0);
}
